/*import React from 'react';

class Dictionary extends Component {
  constructor(props) {
    super(props);

    this.state = { werd: '' };
  }

  render() {
    return  (
      <div className="dictionary">
        <input
         value={this.state.werd}
         onChange={event => this.onDictionaryChange(console.log(value))} />
       </div>
    );
  }

  onInputChange(term) {
   this.setState({term});
   this.props.onSearchTermChange(term);
  }
}

export default Dictionary;*/
